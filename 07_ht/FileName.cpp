#include <iostream>

using namespace std;

int main() {
    
    //1,2
    /*const int row = 4, col = 3;
    int arr[row][col]{};
    int initial,initial2;

    cout << "Enter the start value: ";
    cin >> initial;

    cout << "Enter the start to second value: ";
    cin >> initial2;

    int value = initial;
    int value2 = initial2;
    for (int i = 0; i < row; i++) {
        for (int j = 0; j < col; j++) {
            arr[i][j] = value;
            value *= 2;
        }
    }
    for (size_t i = 0; i < row; i++)
    {
        for (size_t j = 0; j < col; j++)
        {
            cout << arr[i][j] << "\t";
        }
        cout << endl;
    }

    cout << "\n\n====================================\n\n";

    for (int i = 0; i < row; i++) {
        for (int j = 0; j < col; j++) {
            arr[i][j] = value2;
            value2 += 1;
        }
    }

    for (size_t i = 0; i < row; i++)
    {
        for (size_t j = 0; j < col; j++)
        {
            cout << arr[i][j] << "\t";
        }
        cout << endl;
    }*/


}